<a href="#" class="list-group-item list-group-item-action">
  <div class="d-flex justify-content-between">
    <h5 class="mb-1"> {{this.title}} </h5>
    <small>Created {{timeBetween this.date_created}} </small>
  </div>
  <p class="mb-1"> {{shorten text}} </p>
  {{!-- {{#if this.flash_date}}
    <small>  Assigned for  {{this.flash_date}} </small>
  {{else}}
    <small> Just remember this <i class="fas fa-smile-wink yellow"></i> </small>
  {{/if}} --}}
  {{#if this.flash_date}}
    <small>{{flashMessage flash_date}}</small>
  {{/if}}
</a>